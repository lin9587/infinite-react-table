import React from 'react'
import ReactDOM from 'react-dom/client'
import Table from '../table-plus'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <Table />
  </React.StrictMode>
)
